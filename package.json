{
  "name": "decentraland",
  "scripts": {
    "codegen": "graph codegen data-source.yaml",
    "build": "graph build data-source.yaml",
    "build-ipfs": "graph build --ipfs /ip4/127.0.0.1/tcp/5001 data-source.yaml",
    "build-wast": "graph build -t wast data-source.yaml"
  },
  "devDependencies": {
    "the-graph-wasm": "ssh://git@github.com:graphprotocol/the-graph-wasm"
  }
}
